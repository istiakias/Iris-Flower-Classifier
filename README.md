# Iris Species Classifier 🌸

A Machine Learning pipeline that uses the **Decision Tree** algorithm to classify Iris flowers into three species: *Setosa*, *Versicolor*, and *Virginica*.

## 🚀 Project Overview
This project demonstrates the fundamental steps of a machine learning workflow:
1. **Data Loading:** Utilizing the Scikit-Learn built-in Iris dataset.
2. **Preprocessing:** Splitting data into training and testing sets.
3. **Modeling:** Training a Decision Tree Classifier.
4. **Evaluation:** achieving high accuracy on unseen data.
5. **Serialization:** Saving the trained model using Joblib.

## 🛠️ Requirements
* Python 3.x
* Scikit-Learn
* Pandas
* Joblib

## 💻 How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
